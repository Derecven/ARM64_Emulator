#ifndef INSTRUCTIONSEXECUTOR_H
#define INSTRUCTIONSEXECUTOR_H

#include <iostream>
#include <fstream>
#include <vector>
#include <map>
#include <string>

// Function prototype for ExecuteInstructions
void ExecuteInstructions(std::vector<std::vector<std::string>> &vectorOfVectors);

#endif // INSTRUCTIONSEXECUTOR_H
